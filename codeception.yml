# global codeception file to run tests from all apps
include:
    - common
    - frontend
    - frontend/themes/basic
    - backend
    # - api
paths:
    log: console/runtime/logs
settings:
    colors: true